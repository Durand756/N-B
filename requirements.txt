# Core Flask dependencies
Flask==2.3.3
Werkzeug==2.3.7

# HTTP requests
requests==2.31.0
urllib3==2.0.7

# OpenAI API client (compatible with Mistral API)
openai==1.3.7

# JSON handling (usually built-in but explicit for safety)
# json is built-in to Python

# Threading and datetime (built-in)
# threading, datetime are built-in

# Logging (built-in)
# logging is built-in

# Environment variables (built-in)
# os is built-in

# Additional useful packages for production
gunicorn==21.2.0
python-dotenv==1.0.0

# For better JSON handling if needed
simplejson==3.19.2

# Security and CORS (optional but recommended)
flask-cors==4.0.0

# Rate limiting (optional but useful for production)
flask-limiter==3.5.0

# Better error handling
sentry-sdk[flask]==1.38.0

# Development and debugging (optional)
python-json-logger==2.0.7
